DOM :- Document Object Model 

Document is an object created by browser.

if we are doing some chages every time recreate the dom.-> ReactJS.
 
Method of DOM :- 
1. getElementById("demo")
2. getElementByClassName("c1")
3. getElementByTagName("h1")
4. getElementByName("dad")

CSS method :-   css , id -> # and class -> .
1. querySelector("#demo") , (".c1")
2. querySelectorAll


============================================

1. check number is prime or not using the function.
2. Check the number it is armstrong or not.
3. print the faboncci series.
4. Check the highest element from the array.
5. Sort the Array without using the methods.


==================================


Events :- 